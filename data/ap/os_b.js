export const osChaptersB = [
{
 id: 'process-synchronization', title: 'Process Synchronization', blocks: [
 { type: 'text', text: `Concurrent processes can interleave at any instruction. This chapter makes shared data safe: you will recognize races, state the critical-section requirements, and use locks, semaphores, and monitors.` },
 { type: 'heading', text: `Races and critical sections` },
 { type: 'code', code: `// Producer\nwhile (true) {\n  while (counter == BUFFER_SIZE) { }\n  buffer[in] = next_produced;\n  in = (in + 1) % BUFFER_SIZE;\n  counter++;\n}\n// Consumer\nwhile (true) {\n  while (counter == 0) { }\n  next_consumed = buffer[out];\n  out = (out + 1) % BUFFER_SIZE;\n  counter--;\n}` },
 { type: 'text', text: `The full-buffer counter is shared. Increment and decrement are normally load, modify, store sequences. If a producer reads 5 and prepares 6 while a consumer reads 5 and prepares 4, the last store can leave 4 or 6 rather than 5. This timing-dependent wrong result is a race condition; the empty loops also busy-wait.` },
 { type: 'code', code: `do {\n  // entry section: request permission\n  // critical section: access shared data\n  // exit section: release permission\n  // remainder section\n} while (true);` },
 { type: 'text', text: `The critical-section diagram is this loop in order: entry, critical section, exit, remainder. A correct protocol gives mutual exclusion (one participant inside), progress (an eligible participant is not delayed forever by nonparticipants), and bounded waiting (a requester is bypassed only a bounded number of times).` },
 { type: 'heading', text: `Peterson's solution and hardware locks` },
 { type: 'code', code: `// Process Pi; j is the other process\ndo {\n  flag[i] = true;\n  turn = j;\n  while (flag[j] && turn == j) { }\n  // critical section\n  flag[i] = false;\n  // remainder section\n} while (true);` },
 { type: 'text', text: `Peterson's two-process algorithm assumes atomic loads and stores. flag says a process wants entry; turn breaks contention. It meets all three requirements under that model, but real systems rely on atomic hardware operations and memory-order guarantees.` },
 { type: 'code', code: `boolean test_and_set(boolean *target) {\n  boolean old = *target; *target = true; return old;\n}\nboolean lock = false;\ndo {\n  while (test_and_set(&lock)) { }\n  // critical section\n  lock = false;\n} while (true);` },
 { type: 'text', text: `test-and-set reads the old value and sets true indivisibly. The caller that sees false enters; the others spin. A waiting-array variant passes permission to the next waiting process for bounded waiting. Spin locks are for very short sections, including interrupt paths; disable local interrupts before taking a lock that a local interrupt handler also takes, or that handler can spin while its interrupted owner cannot run.` },
 { type: 'code', code: `DEFINE_SPINLOCK(my_lock);\nspin_lock(&my_lock);\n// critical section\nspin_unlock(&my_lock);\n\nDEFINE_RWLOCK(my_rwlock);\nread_lock(&my_rwlock);  // many readers allowed\nread_unlock(&my_rwlock);\nwrite_lock(&my_rwlock); // one writer\nwrite_unlock(&my_rwlock);` },
 { type: 'text', text: `Atomic counters offer indivisible set, add, and increment. Reader-writer locks admit simultaneous readers but exclude a writer; do not upgrade a held read lock to a write lock, because contending upgraders can deadlock.` },
 { type: 'heading', text: `Semaphores and mutexes` },
 { type: 'code', code: `wait(S) { while (S <= 0) { } S--; } // P or down\nsignal(S) { S++; }                      // V or up\n\ntypedef struct { int value; struct process *list; } semaphore;\nwait(S) { S->value--; if (S->value < 0) { add_to(S->list); block(); } }\nsignal(S) { S->value++; if (S->value <= 0) { P = remove(S->list); wakeup(P); } }` },
 { type: 'text', text: `A counting semaphore has an unrestricted integer value; a binary semaphore, or mutex, has 0 or 1. Its wait and signal operations must be atomic. The blocking implementation replaces wasteful spinning with a queue: block suspends the caller and wakeup moves one waiter to the ready queue.` },
 { type: 'heading', text: `Classic synchronization problems` },
 { type: 'code', code: `// Bounded buffer: mutex = 1, full = 0, empty = n\n// producer\nwait(empty); wait(mutex);\n// insert item\nsignal(mutex); signal(full);\n// consumer\nwait(full); wait(mutex);\n// remove item\nsignal(mutex); signal(empty);` },
 { type: 'text', text: `empty prevents overfilling, full prevents consuming nothing, and mutex protects the buffer structure. Reserve an item or slot before taking the short mutex.` },
 { type: 'code', code: `// Readers-writers: rw_mutex = 1, mutex = 1, read_count = 0\n// writer: wait(rw_mutex); /* write */ signal(rw_mutex);\n// reader\nwait(mutex); read_count++;\nif (read_count == 1) wait(rw_mutex);\nsignal(mutex); /* read */\nwait(mutex); read_count--;\nif (read_count == 0) signal(rw_mutex);\nsignal(mutex);` },
 { type: 'text', text: `The first reader excludes writers and the last releases them. Reader-preference can starve writers; writer-preference can delay readers, so fairness is a policy choice.` },
 { type: 'code', code: `// Five dining philosophers; every chopstick starts at 1\ndo {\n  wait(chopstick[i]);\n  wait(chopstick[(i + 1) % 5]);\n  // eat\n  signal(chopstick[i]);\n  signal(chopstick[(i + 1) % 5]);\n  // think\n} while (true);` },
 { type: 'text', text: `The table diagram places five philosophers around a shared bowl, with one chopstick between each pair. If all take their left chopstick first, each waits forever for a right one. Break circular wait by ordering chopsticks, limiting attempts to four, or using a monitor.` },
 { type: 'heading', text: `Practical APIs and recap` },
 { type: 'code', code: `pthread_mutex_lock(&lock);\n// critical work\npthread_mutex_unlock(&lock);\n\nsem_init(&mutex, 0, 1);\nsem_wait(&mutex);\n// critical section\nsem_post(&mutex);\nsem_destroy(&mutex);` },
 { type: 'text', text: `A monitor packages shared state, procedures, implicit mutual exclusion, and condition variables. POSIX mutexes protect one thread; POSIX semaphores use pshared = 0 for threads of one process and shared memory for processes. Incorrect semaphore order, a repeated wait, or a missing wait or signal can cause deadlock or starvation. Choose a primitive that protects both safety and liveness.` },
]},
{
 id: 'deadlocks', title: 'Deadlocks', blocks: [
 { type: 'text', text: `Synchronization prevents races, but a group can still wait forever. Here you will model deadlock, avoid unsafe grants, detect stuck processes, and choose recovery.` },
 { type: 'heading', text: `Characterizing deadlock` },
 { type: 'code', code: `// P0                 // P1\nwait(A);               wait(B);\nwait(B);               wait(A);\n// neither reaches its release` },
 { type: 'text', text: `Deadlock is a set of blocked processes where each holds a resource and waits for one held by another in the set. A one-lane bridge has the same shape: clearing it may require backing one vehicle up, which itself can cause starvation.` },
 { type: 'list', items: [`Mutual exclusion: a nonshareable resource has one user.`, `Hold and wait: a process holds resources while requesting more.`, `No preemption: a holder releases only voluntarily.`, `Circular wait: each process waits for a resource held by the next in a closed chain.`] },
 { type: 'text', text: `All four conditions must hold. A process requests, uses, then releases instances of resource types such as CPU time, memory, and I/O devices.` },
 { type: 'heading', text: `Resource-allocation graphs` },
 { type: 'text', text: `The graph has circular process nodes and rectangular resource-type nodes; each dot inside a rectangle is one instance. A process-to-resource arrow is a request; a resource-to-process arrow is an assignment. In the example, P1 requests R1, R1 is assigned to P2, P2 requests R3, and R3 is assigned to P3; two R2 instances are assigned while R4's three instances are free.` },
 { type: 'text', text: `No cycle means no deadlock. With one instance per resource type, a cycle means deadlock. With several instances, a cycle means only possible deadlock because another instance may become free.` },
 { type: 'heading', text: `Prevention and avoidance` },
 { type: 'list', items: [`Prevention breaks a necessary condition: share where possible; require all resources before running or while holding none; preempt held resources after an unsatisfied request; or number resource types and require increasing request order.`, `Avoidance requires maximum claims and grants only requests that preserve a safe state.`, `Detection permits deadlock and repairs it later; some systems ignore rare deadlocks when that cost is lower.`] },
 { type: 'text', text: `A safe state has a safe sequence: each process's remaining need can be satisfied by available resources plus releases from earlier processes. The state diagram has a safe region, an unsafe region, and a smaller deadlock region within unsafe space. Unsafe is a warning, not automatically a deadlock.` },
 { type: 'heading', text: `Banker's algorithm` },
 { type: 'list', items: [`Available[j] is free instances of resource j.`, `Max[i,j] is Pi's maximum claim.`, `Allocation[i,j] is Pi's current holding.`, `Need[i,j] = Max[i,j] - Allocation[i,j].`] },
 { type: 'code', code: `Work = Available; Finish[i] = false for every i;\nwhile (an unfinished i has Need[i] <= Work) {\n  Work += Allocation[i]; Finish[i] = true;\n}\n// all finished means safe\n\nif (Request[i] <= Need[i] && Request[i] <= Available) {\n  pretend: Available -= Request[i];\n  Allocation[i] += Request[i]; Need[i] -= Request[i];\n  grant only if the resulting state is safe; otherwise restore;\n}` },
 { type: 'text', text: `The safety test imagines a process finishing and returning its allocation. In the three-resource example, Available is (3,3,2); Need rows are (7,4,3), (1,2,2), (6,0,0), (0,1,1), (4,3,1). P1, P3, P4, P2, P0 is safe. P1 requesting (1,0,2) leaves (2,3,0) and still has a safe sequence.` },
 { type: 'heading', text: `Detection and recovery` },
 { type: 'text', text: `For one instance each, collapse resources into a wait-for graph with Pi to Pj when Pi waits for Pj; a cycle is deadlock. With many instances, initialize Work to Available, mark zero-allocation processes finished, repeatedly finish any process whose current Request is at most Work, and add its Allocation to Work. Unfinished processes are deadlocked; the cost is O(m times n squared).` },
 { type: 'text', text: `Run detection at a frequency balanced against its cost and the number of processes likely to need rollback. Recover by aborting all deadlocked processes or one victim at a time. Consider priority, work completed and remaining, resources held and needed, number of victims, and whether work is interactive. Resource preemption needs rollback and a policy that avoids repeatedly victimizing one process.` },
 { type: 'text', text: `Recap: identify the four conditions and graph cycle, then deliberately prevent a condition, preserve safety with Banker's algorithm, or detect and recover.` },
]},
{
 id: 'main-memory-management', title: 'Main Memory Management', blocks: [
 { type: 'text', text: `Running code needs a safe path from its logical addresses to real memory. This chapter develops address binding, placement, paging, page tables, the translation lookaside buffer, and segmentation.` },
 { type: 'heading', text: `Address binding and relocation` },
 { type: 'text', text: `A CPU makes logical addresses; physical addresses reach memory. Binding may occur at compile time with absolute code, at load time with relocatable code, or at execution time when hardware mapping allows a process to move. The processing pipeline is source, compiler or assembler, object module, linker with other objects, load module, loader with libraries, then the in-memory image; dynamic libraries can join at execution time.` },
 { type: 'text', text: `Base and limit registers define legal memory. The shown layout has base 300040 and limit 120900. An MMU example adds relocation 14000 to logical address 346 to form physical 14346; an out-of-range address traps.` },
 { type: 'heading', text: `Swapping and contiguous allocation` },
 { type: 'text', text: `Swapping transfers a whole process between main memory and a backing store. The diagram swaps P1 out and P2 in; transfer time dominates. In multiple partitions, free holes sit among resident system and process regions. First fit takes the first sufficient hole, best fit the smallest sufficient hole, and worst fit the largest; first and best fit usually beat worst fit.` },
 { type: 'text', text: `External fragmentation is enough total free memory but not one suitable contiguous hole. Internal fragmentation is unused space inside an oversized allocation. Compaction combines holes but requires dynamic execution-time relocation.` },
 { type: 'heading', text: `Paging and protection` },
 { type: 'text', text: `Paging splits logical memory into equal pages and physical memory into equal frames. Any free frames can hold a process, and a page table translates page number p to frame f while copying displacement d unchanged. In the 32-byte, 4-byte-page example, pages 0, 1, 2, 3 map to frames 5, 6, 1, 2. A free-frame list allocates four available frames and records them in the new page table.` },
 { type: 'text', text: `A valid-invalid bit protects an address space. In the illustrated table, pages 0 through 5 map validly to frames 2, 3, 4, 7, 8, 9, while pages 6 and 7 are invalid and must trap rather than access another process.` },
 { type: 'heading', text: `Page-table structures and TLB` },
 { type: 'text', text: `Large tables can be hierarchical, hashed, or inverted. A translation lookaside buffer, or TLB, is a fast associative cache of recent page-to-frame entries: a hit avoids a page-table memory lookup; a miss fetches the table entry then may cache it.` },
 { type: 'text', text: `In 32-bit x86 with 4-kilobyte pages, a linear address divides into 10-bit directory index, 10-bit table index, and 12-bit offset; CR3 identifies the page directory. A 4-megabyte page can bypass a table. x86-64 uses four levels and CR3 holds the top-level address and cache-control bits.` },
 { type: 'heading', text: `Segmentation` },
 { type: 'text', text: `Segmentation reflects logical units such as code, stack, data, heap, BSS, read-only data, subroutines, and symbol tables. The diagram labels a subroutine as segment 0, square-root routine 1, main 2, stack 3, and symbol table 4; each occupies a noncontiguous physical range. A segment table supplies base and limit, such as base 1400 and limit 1000 for segment 0.` },
 { type: 'text', text: `Hardware receives (segment, displacement), checks displacement against that segment's limit, then adds its base; failure raises an addressing-error trap. Segmentation can precede paging: a selector creates a linear address, then page-directory and table indexes map it to physical memory.` },
 { type: 'list', items: [`Binding decides when addresses become physical.`, `Contiguous allocation fragments; paging avoids external fragmentation.`, `The TLB accelerates frequent translations.`, `Segmentation exposes meaningful program units and bounds.`] },
]},
{
 id: 'virtual-memory', title: 'Virtual Memory', blocks: [
 { type: 'text', text: `Paging maps resident pages. Virtual memory lets a logical address space exceed physical memory by keeping only needed pages resident; you will trace faults, compare replacement policies, and prevent thrashing.` },
 { type: 'heading', text: `Demand paging` },
 { type: 'text', text: `A process sees contiguous addresses from zero while physical memory is frames. Its virtual layout can contain code, data, an upward-growing heap, a downward-growing stack, and shared-library pages. The shared-library diagram maps two processes' library regions to one physical set of shared pages, so read-only code is not duplicated.` },
 { type: 'text', text: `Demand paging uses a pager, which loads a page only on reference. A page-table valid bit means resident; an invalid entry either identifies an illegal reference, which aborts, or a legal but absent page, which causes a page fault.` },
 { type: 'code', code: `on page_fault(address):\n  if address is illegal: abort\n  find a free frame\n  read missing page from backing store into it\n  update page and frame tables; mark valid\n  restart the faulting instruction` },
 { type: 'text', text: `The page-fault diagram is a six-step route: reference invalid entry, trap to the operating system, locate the page on backing store, bring it to a free frame, reset the table entry, and restart the instruction. The program's behavior does not change.` },
 { type: 'heading', text: `Performance and backing storage` },
 { type: 'text', text: `Effective access time, EAT, is (1 - p) times memory access plus p times page-fault service, where p is the fault rate. With 200-nanosecond memory and 8-millisecond fault service, EAT = 200 + p times 7,999,800 nanoseconds. One fault in 1,000 produces 8.2 microseconds, about forty times slower; under 10 percent degradation needs fewer than one fault per 400,000 accesses.` },
 { type: 'text', text: `Swap space often has faster I/O than file systems. Older systems copied an entire image there at load time; current designs commonly page executable files from their original file and discard clean pages. Anonymous stack or heap pages and dirty file pages need backing storage. Many mobile systems reclaim clean read-only pages instead of swapping.` },
 { type: 'heading', text: `Replacement algorithms` },
 { type: 'text', text: `No free frame means choose a victim, write it only if dirty, mark its old entry invalid, load the demanded page, update mappings, and restart. Algorithms are compared on reference strings. For 7, 0, 1, 2, 0, 3, 0, 4, 2, 3, 0, 3, 0, 3, 2, 1, 2, 0, 1, 7, 0, 1 with three frames, repeated residents are hits.` },
 { type: 'text', text: `First-in, first-out, or FIFO, evicts the oldest resident via a queue. Its trace loads 7, 0, 1; reference 2 replaces 7; later faults evict in arrival order. It has 15 faults on this string. Optimal evicts the page with the farthest next use and has 9 faults here, but cannot know the future; it is a benchmark.` },
 { type: 'text', text: `Least recently used, or LRU, evicts the page whose last use is farthest in the past and has 12 faults here. Store a timestamp in each entry and select the smallest, or keep pages in a doubly linked recency stack and move each reference to the top. Second chance, or clock, circles through frames: select a 0 reference bit, but clear a 1 and advance. Enhanced clock ranks (reference, modify) as (0,0), (0,1), (1,0), (1,1).` },
 { type: 'list', items: [`Least-frequently used evicts the smallest reference count; most-frequently used treats a small count as a possible newcomer.`, `Page buffering maintains free frames, can write dirty victims when idle, and may retain freed contents in case they are needed again.`, `Databases may have better access knowledge and avoid duplicate buffering with raw-disk access.`] },
 { type: 'heading', text: `Allocation and thrashing` },
 { type: 'text', text: `Frames may be divided equally, proportionally to process size, or by priority. Global replacement can take any process's frame and improves throughput but makes execution times variable; local replacement only chooses the faulting process's frames and is more predictable.` },
 { type: 'text', text: `Thrashing is constant paging because a process lacks its active pages. The CPU-utilization graph rises with multiprogramming, then collapses in the thrashing region as paging dominates. The locality model says processes use a working set for a time, then move to another; thrashing occurs when all active localities exceed memory. Limit multiprogramming or use local and priority replacement.` },
 { type: 'text', text: `A process address space is represented by a memory descriptor and divided into virtual memory areas, or VMAs. Each VMA is a contiguous interval such as a mapped file or user stack, with its own permissions and operations.` },
 { type: 'list', items: [`Demand paging is effective only when faults are rare.`, `FIFO is simple, optimal is a benchmark, and LRU uses past behavior.`, `Working-set-aware allocation prevents thrashing.`] },
]},
{
 id: 'file-systems-basics', title: 'File Systems: Basics', blocks: [
 { type: 'text', text: `Files turn temporary storage into persistent, named data. You will follow an open file through its metadata, navigate directory designs, mount remote storage, and apply protection.` },
 { type: 'heading', text: `File concept and operations` },
 { type: 'text', text: `A file is a contiguous logical address space containing numeric, character, binary, program, source, or executable data. Attributes include human-readable name, unique identifier, type, device location, size, protection, times, and user identification; directory structures retain this metadata.` },
 { type: 'list', items: [`Core operations are create, open, close, read, write, seek, delete, and truncate.`, `An open-file table records open files, each process's current pointer and access rights, an open count, and cached disk location.`, `Opening finds a directory entry and moves needed data to memory; closing updates the on-disk directory state.`] },
 { type: 'text', text: `The descriptor diagram has a per-process descriptor table leading to system open-file objects, which lead to inode entries. Several descriptors can share an open object, while several open objects can name one inode. The inode is persistent metadata; the open object carries nonpersistent offset and status.` },
 { type: 'heading', text: `Locks and access methods` },
 { type: 'code', code: `flock(fd, LOCK_SH);                 // shared whole-file lock\nflock(fd, LOCK_EX | LOCK_NB);       // nonblocking exclusive attempt\nflock(fd, LOCK_UN);\n\nlseek(fd, 10, SEEK_SET);\nlockf(fd, F_LOCK, 5);                // bytes [10, 15)\nlockf(fd, F_ULOCK, 5);` },
 { type: 'text', text: `Shared locks permit readers together; exclusive locks permit one writer. Mandatory locking denies conflicting access, while advisory locking relies on cooperating programs. Whole-file locks attach to a file object; POSIX record locks can cover byte ranges and attach to an inode and process, so closing any descriptor for that file can release the process's lock.` },
 { type: 'text', text: `Sequential access supplies read next, write next, and reset. Direct access treats a file as fixed-size logical records and supports read n, write n, and position n, where n is a relative block. Extensions can suggest file type but are not universally authoritative.` },
 { type: 'heading', text: `Directory structures` },
 { type: 'list', items: [`A single-level directory is simple but causes naming and grouping conflicts.`, `A two-level structure gives each user a separate directory and permits same names, but limited grouping.`, `A tree supports a current working directory plus absolute and relative paths; deleting a directory may delete its rooted subtree.`, `An acyclic graph permits shared files or subdirectories; links need back pointers or counts to prevent dangling names.`, `A general graph can contain cycles, so forbid directory links, collect garbage, or detect cycles at link creation.`] },
 { type: 'text', text: `The tree figure branches from root through named directories to file leaves. The shared-subtree figure has two parents reaching one directory, demonstrating aliasing. A directory is typically a list or table of names and pointers to file metadata.` },
 { type: 'heading', text: `Mounting, sharing, and protection` },
 { type: 'text', text: `A filesystem is mounted by attaching its root at a mount point, replacing the previously visible subtree there. Clients can mount a server directory across a network; normal file calls become remote calls. Network File System, or NFS, is a common Unix-style approach. Network and server failures add failure modes; stateless requests carry full arguments and are easier to retry.` },
 { type: 'text', text: `Consistency semantics specify when concurrent users see writes. Unix-style sharing can expose writes immediately to users of a shared open file and share its pointer; session semantics can defer visibility until close.` },
 { type: 'list', items: [`Owners control read, write, execute, append, delete, and list rights.`, `Unix-like modes specify read, write, execute bits for owner, group, and public. Mode 761 means owner read/write/execute, group read/write, public execute.`, `Access control lists name users or groups and allow or deny individual rights.`] },
 { type: 'text', text: `Recap: a file combines bytes, attributes, and operations; directories provide structured names; mounts create one view across file systems; and locks plus permissions make sharing controlled.` },
]},
{
 id: 'file-system-implementation', title: 'File System Implementation', blocks: [
 { type: 'text', text: `The file interface is only the surface. This chapter follows the virtual file system layer, metadata objects, allocation strategies, free-space tracking, recovery, and remote access.` },
 { type: 'heading', text: `Virtual file system structure` },
 { type: 'text', text: `The virtual file system, or VFS, gives applications one API over different local and remote file systems. A write travels from user write() through the system-call and VFS layers to a filesystem-specific write method and physical media. The VFS diagram fans out from one interface to multiple local filesystem types and a remote filesystem over a network.` },
 { type: 'list', items: [`A superblock represents a mounted filesystem or partition.`, `An inode represents one file or directory's metadata.`, `A dentry is a cached directory entry for path lookup.`, `A file object represents an open file for a process.`] },
 { type: 'text', text: `Operation tables provide dispatch: superblock operations synchronize filesystem metadata; inode operations create or link; dentry operations compare and delete; file operations read and write. A superblock records block size, limits, mounted root, type, dirty state, references, and methods. A file object holds offset, flags, path, lock, reference count, and its operation table.` },
 { type: 'heading', text: `Directories and allocation` },
 { type: 'text', text: `A directory may be a linear list of name-to-block pointers: easy to implement but linear to search. Alphabetic order or a B-plus tree helps. Hash tables speed lookup but require collision handling and are awkward with variable-size entries.` },
 { type: 'text', text: `Contiguous allocation records start block and length, giving excellent sequential and direct access but external fragmentation and difficult growth. Linked allocation chains blocks anywhere on disk, avoiding external fragmentation but consuming pointer space, requiring link traversal for random access, and risking corruption from a bad pointer. Clustering reduces seeks but can create internal fragmentation.` },
 { type: 'text', text: `The linked figure follows a file from its directory entry through nonadjacent blocks to a null end. The file allocation table, or FAT, keeps next-block pointers in a table near the volume beginning. A directory's first-block number indexes the table, and the chain can be cached without reading every data block.` },
 { type: 'text', text: `Indexed allocation gives each file an index block of data-block pointers. In the diagram a file points to index block 19, whose entries select blocks 9, 16, 1, 10, and 25; unused entries are -1. It supports direct access without external fragmentation but spends index space.` },
 { type: 'code', code: `// One index block, 512-byte blocks\nQ = logical_address / 512; // index entry\nR = logical_address % 512; // offset inside selected data block\n\n// Multi-index mapping\nQ1 = logical_address / (512 * 511);\nR1 = logical_address % (512 * 511);\nQ2 = R1 / 512; R2 = R1 % 512;` },
 { type: 'text', text: `A two-level 4-kilobyte index with four-byte pointers addresses about one million blocks, roughly 4 gigabytes. Unix-style combined indexing uses direct pointers first, then single-, double-, and triple-indirect blocks for larger files.` },
 { type: 'heading', text: `Free space and recovery` },
 { type: 'text', text: `A bitmap uses one bit per block, making contiguous runs easy to find. A 1-terabyte disk with 4-kilobyte blocks needs 2 to the 28 bits, or 32 megabytes; four-block clusters reduce it to 8 megabytes. A free list chains free blocks and wastes no bitmap space, but makes contiguous runs hard to locate.` },
 { type: 'text', text: `Consistency checking compares directory metadata and disk blocks, but can be slow or incomplete; backups permit restoration. Journaling writes metadata changes as sequential transactions to a log, commits them there, then later applies them to main structures. After a crash, replay remaining logged transactions to restore consistency quickly.` },
 { type: 'text', text: `NFS mounts a remote directory after an export-list check and returns a file handle containing a filesystem identifier and inode number. The client VFS sends remote procedure calls through the network to a server VFS and disk. Traditional NFS is stateless: each request supplies full arguments for lookup, directory reading, metadata, and file reads or writes.` },
 { type: 'list', items: [`VFS separates generic calls from filesystem-specific implementation.`, `Contiguous allocation is fast but fragments; linked allocation grows easily but has poor random access; indexed allocation trades index space for direct access.`, `Bitmaps, free lists, journals, checks, and backups manage capacity and failure.`] },
]},
{
 id: 'input-output-systems', title: 'Input/Output Systems', blocks: [
 { type: 'text', text: `Input and output connects computation to storage, networking, displays, and people. You will follow requests through device hardware and drivers, choose polling, interrupts, or direct memory access, and understand block-I/O scheduling.` },
 { type: 'heading', text: `I/O hardware and device control` },
 { type: 'text', text: `Devices include storage, transmission, and human-interface equipment. A port is a connection point, a bus is a shared or chained pathway, and a controller or host adapter operates a device with processor logic, private memory, microcode, and bus control. Drivers encapsulate those details behind a uniform interface.` },
 { type: 'text', text: `The bus figure has a central PCI bus joining processor, bridge-memory controller, cache, memory, graphics controller and monitor, disk controllers and disks, and an expansion-bus interface. The slower expansion bus connects devices such as keyboard and serial or parallel ports.` },
 { type: 'text', text: `Drivers command devices through data-in, data-out, status, and control registers, often one to four bytes or a first-in-first-out buffer. Registers are reached by direct I/O instructions or memory-mapped I/O, where they occupy processor address space.` },
 { type: 'heading', text: `Polling, interrupts, and DMA` },
 { type: 'code', code: `while (status_register.busy == 1) { }\nset_read_or_write_bit();\nif (writing) copy_to_data_out_register();\nset_command_ready();\n// controller transfers, then clears busy, error, and command-ready` },
 { type: 'text', text: `Polling repeatedly reads a busy bit, masks it, and branches. It is reasonable for a fast device or short wait, but wastes processor cycles on slow I/O and can miss data if the CPU cannot check often enough.` },
 { type: 'text', text: `An interrupt-request line lets a device notify the processor after a transfer or error. The CPU checks between instructions, saves context, uses an interrupt vector to dispatch the right handler, then restores work. Interrupts can be prioritized, maskable, nonmaskable, or chained on a shared number; related traps handle exceptions, page faults, and system calls.` },
 { type: 'text', text: `The interrupt-cycle diagram is a loop: driver initiates I/O, controller starts I/O, readiness/completion/error raises an interrupt, CPU transfers to a handler, handler processes it and returns, and CPU resumes the interrupted task.` },
 { type: 'text', text: `Direct memory access, or DMA, moves large transfers without byte-at-a-time programmed I/O. The operating system puts source, destination, direction, and byte count in a command block and gives its address to the DMA controller. It masters the bus, transfers device-to-memory or memory-to-device, and interrupts at completion; cycle stealing is still far cheaper than CPU copying.` },
 { type: 'text', text: `The DMA figure's six steps are: driver supplies buffer address and count; controller learns the command block; disk controller starts DMA; it supplies bytes; DMA writes the buffer while advancing address and reducing count; count zero raises completion interrupt.` },
 { type: 'heading', text: `Interrupt handlers and control` },
 { type: 'code', code: `int request_irq(unsigned int irq,\n  irqreturn_t (*handler)(int, void *, struct pt_regs *),\n  unsigned long flags, const char *devname, void *dev_id);\n\nstatic irqreturn_t handler(int irq, void *dev_id, struct pt_regs *regs) {\n  if (!our_device_caused_interrupt()) return IRQ_NONE;\n  // acknowledge and perform time-critical work\n  return IRQ_HANDLED;\n}` },
 { type: 'text', text: `An interrupt handler, or interrupt service routine, belongs to its driver and must run briefly. irq identifies the line, flags can request shared-line support, and dev_id distinguishes sharers. request_irq may sleep, so it cannot run from interrupt context. A handler returns IRQ_NONE if another device caused the event and IRQ_HANDLED if it serviced its own.` },
 { type: 'text', text: `Interrupt context belongs to no process, cannot sleep, is time-critical, and normally shares the interrupted task's kernel stack. A shared handler requires the shared flag, a unique device identifier, and a check that its device was the source.` },
 { type: 'code', code: `local_irq_save(flags);\n// protect state shared with a local interrupt\nlocal_irq_restore(flags);\n\ndisable_irq(irq);        // waits for a running handler\ndisable_irq_nosync(irq); // does not wait\nsynchronize_irq(irq);    // waits for this handler` },
 { type: 'text', text: `Interrupt control is synchronization. Disabling locally does not exclude other processors, so shared kernel state also needs locks. Saving and restoring is safer than blindly enabling, because interrupts might already have been disabled.` },
 { type: 'heading', text: `I/O layers and block scheduling` },
 { type: 'text', text: `System calls classify devices while drivers hide controller differences. Devices may be character-stream or block, sequential or random, synchronous or asynchronous, sharable or dedicated, and read/write, read-only, or write-only. Character devices transfer ordered streams; block devices transfer indexed multibyte blocks and allow repositioning.` },
 { type: 'text', text: `A bio describes an in-flight block I/O as segments. It points to bio_vec entries; each entry identifies a physical page, byte length, and page offset. Thus one logical operation can use buffers spread through physical memory.` },
 { type: 'text', text: `The block-I/O layer receives filesystem requests, queues them, and sends them through an I/O scheduler to a block-device driver. The layer diagram is VFS, generic block I/O containing the scheduler, then driver. Schedulers merge adjacent sectors and sort requests to reduce disk-head movement, raise throughput, and retain fairness.` },
 { type: 'list', items: [`The traditional elevator merges adjacent requests; otherwise it may dispatch an old request to avoid starvation, insert by sector order, or append at the tail.`, `Deadline scheduling maintains a sorted queue plus read and write deadline queues. It dispatches expired requests so neither waits indefinitely.`, `Anticipatory scheduling briefly waits after a dispatch for a nearby read; it can help server access patterns but harm seek-heavy databases.`, `Completely fair queuing, or CFQ, has a sorted queue per process and serves them round-robin for per-process fairness.`, `No-op scheduling only merges, not sorts, and suits flash or other truly random-access devices with negligible seek cost.`] },
 { type: 'heading', text: `Kernel modules and recap` },
 { type: 'code', code: `int init_my_module(void) {\n  printk("module loaded\\n");\n  return 0;\n}\nvoid exit_my_module(void) { printk("module exiting\\n"); }\nmodule_init(init_my_module);\nmodule_exit(exit_my_module);\nMODULE_LICENSE("GPL");` },
 { type: 'text', text: `Kernel modules are separately compiled components dynamically added to and removed from a running kernel. They have initialization and exit routines, may export symbols, and support drivers, interrupt handlers, filesystems, monitoring, and other features without rebuilding the whole kernel.` },
 { type: 'list', items: [`Polling spends CPU time checking; interrupts notify the CPU; DMA transfers bulk data directly.`, `Handlers must be short, non-sleeping, and correctly synchronized.`, `Drivers present uniform access, and block schedulers merge, order, and fairly dispatch requests.`] },
]},
];
