# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
/build
/out
/.next

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local
plan.txt
.claude/

# Source course material (contains private student submissions) — not for the site
/Python with accessibility A and B/
/lb_webDev/
game.txt
.playwright-mcp/

# Advanced Programming (Java) slide sources and extraction artifacts — not for the site.
# Only the generated data/ap/*.js course files are committed.
/AP slides ppt/
/slides_extracted/
/slides_images/
extract_slides.py
extract_images.py

# Python and Mobile Computing source material and PDF extraction artifacts — not for the site.
/python CSE course/
/Mobile Computing/
/pdf_extracted/
/pdf_pages/
extract_pdfs.py
render_pdfs.py
extract_pptx_extra.py

# Operating Systems slide sources and extraction artifacts — not for the site.
/OperatingSystems/
/os_extracted/
/os_images/
extract_os.py

# Computer Networks source material (copyrighted textbook slides) and extraction
# artifacts — NOT for the site. Only the original, transformed course files ship.
/ComputerNetworks/
/cn_extracted/
/cn_images/
extract_cn.py

# DBMS source material (copyrighted textbook slides) and extraction artifacts —
# NOT for the site. Only the original, transformed course files ship.
/DBMS/
/dbms_extracted/
/dbms_images/
extract_dbms.py

npm-debug.log*
yarn-debug.log*
yarn-error.log*
